import { Bounded } from '@snoomleng/ui';
import React from 'react';

const BlogDetailPage = async (): Promise<React.JSX.Element> => {
  return <Bounded>BlogDetailPage</Bounded>;
};

export default BlogDetailPage;
